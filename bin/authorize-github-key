#!/usr/bin/env sh
#
# Download the public ssh key of a GitHub user and append it to ~/.ssh/authorized_keys.
# Example:
#     authorize-github-key qobilidop
#

curl https://github.com/$1.keys >> ~/.ssh/authorized_keys
