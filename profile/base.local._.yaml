# SSH
- link:
    ~/.ssh/config: local/ssh/config_local
    ~/.ssh/config_remote: local/ssh/config_remote

# Zsh
- shell:
  - which zsh
  - sudo chsh -s "$(which zsh)" "$USER"
  - script/install-oh-my-zsh.sh
  - script/install-powerlevel9k.sh
- link:
  - ~/.zshrc: config/zshrc
